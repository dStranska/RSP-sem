{% extends 'base.twig' %}



{% block body %}

    <div class="row">
        <div class="col-4"></div>
        <div class="col-4">
            <div class="row" style="text-align: center">
                <div class="col-12" style="margin-top:120px">
                    {{ form_start(form) }}

                    {{ form_widget(form) }}

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
        <div class="col-4"></div>
    </div>



{% endblock %}